module.exports = {
  dependencies: {
    "react-native-zetic-mlange": {
      platforms: {
        // Always enable for iOS device builds.
        // Use --device builds; simulator slices may not be available from ZETIC.
        ios: {},
      },
    },
  },
};
